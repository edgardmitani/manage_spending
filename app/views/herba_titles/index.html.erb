<section class="content">
	<div class="row">
		<div class="col-xs-12">
			<div class="box">
				<div class="box-header">
					<h3 class="box-title"><%= t("title.title") %></h3>
					<div class="box-tools">
						<%= link_to t("link.registering"), new_herba_title_path, class: "btn-register btn-default pull-right" %>
					</div>
				</div>
				<div class="box-body table-responsive no-padding">
					<table class="table table-striped">
						<tr>
							<th>ID</th>
							<th><%= t("table.name") %></th>
							<th><%= t("table.discount") %></th>
							<th colspan="2"></th>
						</tr>
						<% @titles.each do |title| %>
							<tr>
								<td><%= title.id %></td>
								<td><%= title.name %></td>
								<td><%= title.discount %>%</td>
								<td><%= link_to "", edit_herba_title_path(title), class: "fa fa-edit", title: t("link.edit") %></td>
								<td><%= link_to "", title, method: :delete, data: { confirm: t("flash_message.are_you_sure") }, class: "fa fa-trash-o", title: t("link.delete") %></td>
							</tr>
						<% end %>
					</table>
				</div>
			</div>
		</div>
	</div>
</section>
